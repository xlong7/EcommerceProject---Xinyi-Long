<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Order</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <style>
        body {
          margin: 0;
          font-family: Arial, Helvetica, sans-serif;
        }
        
        .topnav {
          overflow: hidden;
          background-color: rgb(70, 82, 82);
        }
        
        .topnav a {
          float: left;
          color: #f2f2f2;
          text-align: center;
          padding: 14px 16px;
          text-decoration: none;
          font-size: 17px;
        }
        
        .topnav a:hover {
          background-color: #ddd;
          color: black;
        }
        
        .topnav a.active {
          background-color: #030605;
          color: white;
        }
        .brand1{
          display:none
        }
        .brand2{
          display:none
        }
      </style>
  </head>
  <body>
        
    <div class="topnav">
        <a id="homepage" href="/homepage">Home</a>
        <a id="cart" href="/cartpage">Cart</a>
        <a id="orders" href="/myorder">My Orders</a>
        <div>
        {{#if loggedIn}}
          <a id="logout" href="" onclick=clearCookies() >Logout</a>
        {{else}}
          <a id="register" href="/register">Sign up</a>
          <a id="login" href="/login">Login</a>
        {{/if}}
    </div>
    </div>
    <div style="padding-left:16px">
    </div>
    <div>
      {{#if loggedIn}}
        <ul class="orderList">
        {{#each orders}}
          <li>Order Number:{{_id}}</li>
          <li>Ordered Products: {{products}}</li>
          <li>Purchase Time: {{time}}</li>
          <li>Order Status: {{status}}</li>
          <br/> <br/> <br/>
        {{/each}}  
        </ul>
      {{else}}
        <h1>Please login before access to your order</h1>
      {{/if}}

     </div>
     <script>
      function clearCookies(){
        document.cookie.split(";").forEach(function(c) { document.cookie = c.replace(/^ +/, "").replace(/=.*/, "=;expires=" + new Date().toUTCString() + ";path=/"); });
      }
    </script>
</body>
</html>